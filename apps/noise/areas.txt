# Which areas get noise tiles.
#
#   docker compose -f docker-compose.prod.yml run --rm builder \
#     build-noise-tiles --areas=/opt/wayside/areas.txt
#
# This file is the control. An area not listed here is not built, whatever is
# in cities.json — that file says which areas *exist*, this one says which are
# worth the download and the disk.
#
# GERMANY ONLY, for now. The full list — 60 cities across 25 countries, chosen
# from Matomo usage and from how many noise-relevant points each city has — is
# in the git history of this file and is 31 GB of downloads. Germany is 4.6 GB,
# already cached on the server, and seven cities is enough to see whether the
# boundary clip is doing what it should before spending the rest.
#
# Each of these is clipped to its own administrative boundary plus ten
# kilometres — see boundaries.geojson and NOISE_BOUNDARY_BUFFER — rather than
# to a circle on its centre.

# Germany
berlin
munich
hamburg
stuttgart
cologne
frankfurt
dusseldorf
